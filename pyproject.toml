[build-system]
build-backend = 'setuptools.build_meta'
requires = ['setuptools>=64', 'setuptools_scm>=8']

[dependency-groups]
# The tests run the exporter under the same environment the installer builds.
dev = [
  'cvista[io]',
  'cvista[rendering]',
  'pre-commit',
  'pytest',
  'pytest-cov',
  # Pinned to one commit; bumping it reruns the test that locks the readable extensions.
  'pyvista-cad[step-light,3mf]',
  'pyvista[io] @ git+https://github.com/pyvista/pyvista.git@f96cb9990ec77ba0d12d4d19cba6035e6b1841aa',
]

[project]
authors = [{ name = 'Erik Bedard' }]
classifiers = [
  'Development Status :: 4 - Beta',
  'Environment :: MacOS X',
  'Intended Audience :: Developers',
  'Intended Audience :: Science/Research',
  'Operating System :: MacOS :: MacOS X',
  'Programming Language :: Python :: 3',
  'Programming Language :: Python :: 3.14',
  'Topic :: Scientific/Engineering :: Visualization',
]
dependencies = []
description = 'Quick Look previews for PyVista-readable mesh files.'
dynamic = ['version']
keywords = ['macos', 'mesh', 'preview', 'pyvista', 'quicklook', 'vtk']
license = 'MIT'
name = 'pyvista-quicklook'
readme = 'README.md'
# One version throughout: the helper and the PyVista environment are the same Python.
requires-python = '>=3.14,<3.15'

[project.scripts]
pvql = 'pyvista_quicklook.cli:main'
# The render service runs under this name, which is what macOS shows for it.
pyvista-quicklook = 'pyvista_quicklook.cli:main'

[project.urls]
Homepage = 'https://github.com/user27182/pyvista-quicklook'

[tool.coverage.run]
branch = true
omit = ['*/_formats_table.py', '*/_version.py', '*/_warmup.py']
parallel = true
source = ['pyvista_quicklook']

[tool.pytest.ini_options]
addopts = '--cov=pyvista_quicklook --cov-fail-under=70'
testpaths = 'tests'

[tool.ruff]
indent-width = 4
line-length = 99

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 75
quote-style = 'single'

[tool.ruff.lint]
extend-select = [
  'A',
  'B',
  'C4',
  'D',
  'E',
  'EM',
  'F',
  'FA',
  'I',
  'ICN',
  'N',
  'PERF',
  'PGH',
  'PIE',
  'PT',
  'RET',
  'RSE',
  'RUF',
  'SIM',
  'T10',
  'T20',
  'TCH',
  'TID',
  'UP',
  'W',
  'YTT',
]
ignore = ['D203', 'D211', 'D213']

[tool.ruff.lint.isort]
combine-as-imports = true
force-single-line = true
force-sort-within-sections = true
required-imports = ['from __future__ import annotations']

[tool.ruff.lint.per-file-ignores]
# The command line interface reports its findings on stdout.
'pyvista_quicklook/cli.py' = ['T201']
'tests/*' = ['D103']

[tool.ruff.lint.pyupgrade]
keep-runtime-typing = true

[tool.uv]
# PyVista requires stock VTK, which cvista replaces; without this every resolve installs both.
override-dependencies = ["vtk; python_version < '0'"]

[tool.setuptools]
license-files = ['LICENSE']
packages = ['pyvista_quicklook']

[tool.setuptools_scm]
version_scheme = 'release-branch-semver'
write_to = 'pyvista_quicklook/_version.py'
